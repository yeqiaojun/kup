#include "platform_socket.hpp"

namespace ukcp {

// Windows select fallback is implemented in platform_epoll.cpp through
// conditional compilation so this translation unit stays intentionally empty.

}  // namespace ukcp
