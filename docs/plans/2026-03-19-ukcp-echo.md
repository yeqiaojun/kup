# UKCP Echo Example Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Add a runnable echo server example that shows repeated UDP uplink and KCP uplink sharing one port while replies always use KCP.

**Architecture:** Keep the example tiny. Put the echo behavior in a small handler constructor inside the example package so tests can exercise the same logic the runnable binary uses. Reuse the existing `Client`, `Server`, and `Session.Send` APIs.

**Tech Stack:** Go 1.26, standard library, existing `ukcp` package

---

### Task 1: Add example contract tests

**Files:**
- Create: `D:/tkcp/ukcp/examples/echo/echo_test.go`

**Step 1: Write the failing test for repeated UDP echo**

Start the echo server, send one logical UDP message repeated multiple times, and assert the client receives one KCP echo.

**Step 2: Write the failing test for KCP echo**

Send one KCP message and assert the client receives one KCP echo.

**Step 3: Run test to verify it fails**

Run: `go test ./...`

Expected: fail because the example package does not exist yet.

### Task 2: Implement the example

**Files:**
- Create: `D:/tkcp/ukcp/examples/echo/main.go`

**Step 1: Add the echo handler**

Implement `OnUDP` and `OnKCP` by calling `sess.Send(payload)`.

**Step 2: Add the runnable entrypoint**

Expose a `-listen` flag and block forever after starting the server.

### Task 3: Verify

**Files:**
- Test: `D:/tkcp/ukcp/examples/echo/echo_test.go`

**Step 1: Run the full test suite**

Run: `go test ./...`

Expected: pass.
